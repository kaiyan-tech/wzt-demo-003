# ========================================
# 本地开发环境配置
# ========================================
#
# 使用方法：
# 1. 复制此文件为 .env: cp .env.example .env
# 2. 启动本地数据库: pnpm db:start
# 3. 运行迁移: pnpm db:migrate
# 4. 启动后端: pnpm dev:backend
#
# 注意：此文件仅用于本地开发，生产环境配置通过 GitHub Secrets 管理
# ========================================

# 数据库连接（本地 Docker PostgreSQL）
# 数据库名需与 docker-compose.yml 中的 DB_NAME 一致（默认 ky_framework）
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/ky_framework?schema=public"

# JWT 配置（本地开发使用固定值，生产环境会自动生成）
JWT_SECRET="local-dev-secret-change-in-production"
JWT_EXPIRES_IN="7d"

# 应用配置
NODE_ENV="development"
PORT=3000

# CORS（本地前端地址）
CORS_ORIGIN="http://localhost:5173"

# 迁移令牌（本地开发用固定值）
MIGRATION_TOKEN="local-dev-migration-token"
